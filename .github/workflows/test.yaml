name: yaci tests
on: [push, pull_request]

jobs:
  ubuntu:
    strategy:
      matrix:
        cc: [gcc, clang]
    runs-on: ubuntu-latest
    steps:
      - name: Clone GIT repo
        uses: actions/checkout@v4
      - name: Build tests
        run: |
          if ! pkg-config --exist readline; then make install-readline; fi
          make CC=${{ matrix.cc }}
          make test
